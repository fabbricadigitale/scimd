package mold

import (
	"testing"
)

type normurnTestCase struct {
	val interface{} // input urn
	// (todo) > complete
}

// (todo) > populate
var normurnTests = []normurnTestCase{
	{},
}

func TestNormUrn(t *testing.T) {
	for i, test := range normurnTests {
		// (todo) > complete
	}
}
